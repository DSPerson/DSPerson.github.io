<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Linux命令行学习</title>
    <url>/2016/08/12/Linux-Code/</url>
    <content><![CDATA[<h2 id="scp"><a href="#scp" class="headerlink" title="scp"></a>scp</h2><h3 id="将本地文件添加到远程服务器"><a href="#将本地文件添加到远程服务器" class="headerlink" title="将本地文件添加到远程服务器"></a>将本地文件添加到远程服务器</h3><ul>
<li>我的远程服务器tiger@192.168.0.3</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">scp 本地文件路径 tiger@192.168.0.3:服务器的路径</div></pre></td></tr></table></figure>
<h2 id="删除-rm"><a href="#删除-rm" class="headerlink" title="删除 rm"></a>删除 rm</h2><p>两个参数</p>
<h3 id="f-强制删除"><a href="#f-强制删除" class="headerlink" title="f 强制删除"></a>f 强制删除</h3><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">rm -f</div></pre></td></tr></table></figure>
<h3 id="r-递归删除有多少都删除掉"><a href="#r-递归删除有多少都删除掉" class="headerlink" title="r 递归删除有多少都删除掉"></a>r 递归删除有多少都删除掉</h3><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">rm -rf</div></pre></td></tr></table></figure>
<p>有权限就添加sudo 加上面的命令</p>
<h2 id="git-ssh-问题"><a href="#git-ssh-问题" class="headerlink" title="git ssh 问题"></a>git ssh 问题</h2><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">cd ~/.ssh/</div></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">vim config</div></pre></td></tr></table></figure>
<p>按i 插入</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">Host git.oschina.net</div><div class="line">IdentityFile ~/.ssh/你的ssh公钥不带.pub</div></pre></td></tr></table></figure>
<p>ssh  .ssh/id_rsa id_rsa.pub</p>
]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Mac上搭建phpStorm和Xdebug教程;出现phpinfo()和php -v版本不一致问题</title>
    <url>/2016/09/18/MAC-PHPSTORM-XDEBUG/</url>
    <content><![CDATA[<p>感觉百度都是说废话的。google还是亲儿子。</p>
<a id="more"></a>
<h2 id="别跟我说你电脑没有HomeBrew"><a href="#别跟我说你电脑没有HomeBrew" class="headerlink" title="别跟我说你电脑没有HomeBrew"></a>别跟我说你电脑没有HomeBrew</h2><ul>
<li>如果真没有也没关系；<a href="http://brew.sh/" target="_blank" rel="external">请点击这里安装</a></li>
</ul>
<h2 id="前提是你的PHP环境已经安装完毕"><a href="#前提是你的PHP环境已经安装完毕" class="headerlink" title="前提是你的PHP环境已经安装完毕"></a>前提是你的PHP环境已经安装完毕</h2><ul>
<li>先查看你的php版本号</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">php -v</div></pre></td></tr></table></figure>
<p>我的版本是5.5.38所以下面我用55就可以了你要是5.6就用php56即可</p>
<ul>
<li>安装<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">brew install homebrew/php/php55-xdebug</div></pre></td></tr></table></figure>
</li>
</ul>
<p>loading…..</p>
<p>之后完事你会看到</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">To finish installing xdebug for PHP 5.5:</div><div class="line">  </div><div class="line">  * /usr/local/etc/php/5.5/conf.d/ext-xdebug.ini was created,</div><div class="line">    do not forget to remove it upon extension removal.</div><div class="line">  * Validate installation via one of the following methods:</div><div class="line">  *</div><div class="line">  * Using PHP from a webserver:</div><div class="line">  * - Restart your webserver.</div><div class="line">  * - Write a PHP page that calls &quot;phpinfo();&quot;</div><div class="line">  * - Load it in a browser and look for the info on the xdebug module.</div><div class="line">  * - If you see it, you have been successful!</div><div class="line">  *</div><div class="line">  * Using PHP from the command line:</div><div class="line">  * - Run `php -i &quot;(command-line &apos;phpinfo()&apos;)&quot;`</div><div class="line">  * - Look for the info on the xdebug module.</div><div class="line">  * - If you see it, you have been successful!</div><div class="line">  *</div></pre></td></tr></table></figure>
<p>这么一大堆</p>
<ul>
<li>进入你该去的文件夹</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">cd /usr/local/etc/php/5.5/conf.d/</div></pre></td></tr></table></figure>
<ul>
<li>编辑里面唯一的ext-xdebug.ini这个文件</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">vi ext-xdebug.ini</div></pre></td></tr></table></figure>
<ul>
<li>按 i 进入插入模式 一般这个port端口设置为9000因为phpStorm默认就是这个当然你可以改。将下面一堆复制到里面去记得别把之前的内容覆盖掉<br>&lt;环境配置&gt;<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">xdebug.remote_enable=1</div><div class="line">xdebug.remote_port=9000</div><div class="line">xdebug.remote_handler=&quot;dbgp&quot;</div><div class="line">xdebug.remote_host=&quot;localhost&quot;</div><div class="line">xdebug.idekey=PHPSTORM</div></pre></td></tr></table></figure>
</li>
</ul>
<p>按 esc –&gt; : –&gt; wq 进行保存(箭头(–&gt;)是告诉你步骤)；<br>之后环境就配置完了如果你更改9000这个端口的话。phpStrom中不用设置。如果改了在phpStorm中点击 commond + 逗号，选择 Languages&amp;Frameworks找到PHP下面的Debug有个Xdebug 下面的Debug prot更改即可。</p>
<ul>
<li>最后下载xdebug helper 安装到谷歌浏览器上这个貌似需要科学上网 </li>
</ul>
<p>[此文编辑于2016-9-18]</p>
<p>在Apache log找不到情况下的错误</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">apachectl -k start -e DEBUG</div></pre></td></tr></table></figure>
<h2 id="xdebug安装可能出现的问题"><a href="#xdebug安装可能出现的问题" class="headerlink" title="xdebug安装可能出现的问题"></a>xdebug安装可能出现的问题</h2><h3 id="安装显示成功但是当你phpinfo-界面中不存在xdebug"><a href="#安装显示成功但是当你phpinfo-界面中不存在xdebug" class="headerlink" title="安装显示成功但是当你phpinfo() 界面中不存在xdebug"></a>安装显示成功但是当你phpinfo() 界面中不存在xdebug</h3><h4 id="这个也可以php-v-和phpinfo-中版本不一致问题"><a href="#这个也可以php-v-和phpinfo-中版本不一致问题" class="headerlink" title="这个也可以php -v 和phpinfo()中版本不一致问题"></a>这个也可以php -v 和phpinfo()中版本不一致问题</h4><ul>
<li>有很多人执行</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">curl -s http://php-osx.liip.ch/install.sh | bash -s 5.6</div></pre></td></tr></table></figure>
<ul>
<li>进行更新php产生的xdebug不好使的问题是因为<figure class="highlight"><figcaption><span>```中php的版本和你电脑现在运行的版本不一致。</span></figcaption><table><tr><td class="code"><pre><div class="line">* 打开php的配置文件 位置：```/etc/apache2/httpd.conf```找到```LoadModule php5_module libexec/apache2/libphp5.so``` 其实```apache```一直加载的是系统默认的。</div><div class="line">* ```mdfind -name libphp5.so``` 才发现路径有两个</div></pre></td></tr></table></figure></li>
</ul>
<p>/usr/local/Cellar/php55/5.5.38/libexec/apache2/libphp5.so<br>/usr/libexec/apache2/libphp5.so<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">* 一个是系统默认一个是更新来的 当然我这里不是更新；是我sb来的~~~~(&gt;_&lt;)~~~~</div><div class="line">* 这个时候打开httpd.conf中将```libexec/apache2/libphp5.so```更换为更新后的路径的路径即：</div><div class="line"></div><div class="line">```local/Cellar/php55/5.5.38/libexec/apache2/libphp5.so</div></pre></td></tr></table></figure></p>
<p>last：<code>sudo apachectl restart</code></p>
<p><a href="https://antistatique.net/en/we/blog/2013/09/17/debugging-with-xdebug-and-phpstorm-on-macos-x" target="_blank" rel="external">解决：xdebug安装问题</a></p>
<p><a href="http://stackoverflow.com/questions/14357107/installing-mcrypt-on-osx-10-8-2-php-5-3-15-with-homebrew" target="_blank" rel="external">解决：php -v出现警告问题</a></p>
<p><a href="http://blog.csdn.net/ul646691993/article/details/45769721" target="_blank" rel="external">解决：搜索问题！关键点啊！！！</a></p>
<p><a href="https://www.jetbrains.com/help/phpstorm/2016.2/configuring-xdebug.html" target="_blank" rel="external">官方phpStrom安装xdebug</a></p>
<hr>
<h1 id="此文更新于mac-10-12-后重新使用php"><a href="#此文更新于mac-10-12-后重新使用php" class="headerlink" title="此文更新于mac 10.12 后重新使用php"></a>此文更新于mac 10.12 后重新使用php</h1><blockquote>
<p>背景：自己想做个后台php 移动端iOS 的 项目所有都是自己操刀。之后刚买电脑配置的xdebug又不好用了。</p>
<p>第一步想法：想使用我这篇文章之前的方法，很不幸我失败了，终端报错原因是一个环境的版本过低。</p>
<p>第二步实践:突然间发现一个自己一直误解了Xdebug官网(之前认为之后windows) <a href="https://xdebug.org/wizard.php" target="_blank" rel="external">官网</a>也怪我英语差。。在某个文章发现官网里面的框框是将你的本地的phpinfo()的源网页复制进去他会自动给你分析你所需要的Xdebug。</p>
</blockquote>
<p><img src="/2016/09/18/MAC-PHPSTORM-XDEBUG/Snip20170120_1.png" alt=""></p>
<blockquote>
<p>一共九个步骤 ，我把其中出现问题的列出来和方法</p>
</blockquote>
<pre><code>4: phpize 应该是mac缺少此工具 执行:xcode-select --install 之后点击安装之后在使
用就可以
7: cp modules/xdebug.so /usr/lib/php/extensions/no-debug-non-
zts-20131226 这个方法会报错误，即使你使用sudo 也不行。百度一下是说他是防护罩的最后
一层，之前可以做到但是更新后做出来不能改变(需要再重启command+R等系列操作麻烦的很)
我取巧做了个操作，他的本意是将modules/xdebug.so复制到/usr/lib/php/extensions/
no-debug-non-zts-20131226 但是复制失败。所以我将这个modules/xdebug.so放到了平
常不注意的也不会删除的地方。此处我因为文稿直放一些我需要的文件而且从来不删除所以我放到
了文稿这个地方
</code></pre><p><img src="/2016/09/18/MAC-PHPSTORM-XDEBUG/Snip20170120_2.png" alt=""></p>
<pre><code>8:这个步骤就需要作出相应的改变了。默认是
zend_extension = /usr/lib/php/extensions/no-debug-non-zts-20131226/
xdebug.so(你的可能和我不同) 只需要将/usr/lib/php/extensions/no-debug-non-
zts-20131226/换成你的文稿路径就可以了(我的路径/Users/你本机名字/Documents/
xdebug.so)
</code></pre><blockquote>
<p>如果此处这样写还差了点东西即我此文章上面的信息配置(commond + f 搜索：&lt;环境配置&gt;)复制进去即可</p>
</blockquote>
<p><a href="http://www.codesec.net/view/218436.html" target="_blank" rel="external">解决phpize无法使用问题</a></p>
]]></content>
      <categories>
        <category>PHP环境搭建</category>
      </categories>
      <tags>
        <tag>PHP</tag>
      </tags>
  </entry>
  <entry>
    <title>Git 基本操作学习</title>
    <url>/2016/12/22/Git/</url>
    <content><![CDATA[<blockquote>
<p>坚持真的很重要 仅此而已</p>
</blockquote>
<a id="more"></a>
<h1 id="从创建仓库到推送"><a href="#从创建仓库到推送" class="headerlink" title="从创建仓库到推送"></a>从创建仓库到推送</h1><h2 id="克隆分支"><a href="#克隆分支" class="headerlink" title="克隆分支"></a>克隆分支</h2><pre><code>git clone &lt;url&gt;
</code></pre><h2 id="将代码中所需要的添加进去-一般情况下都添加-即所有的意思"><a href="#将代码中所需要的添加进去-一般情况下都添加-即所有的意思" class="headerlink" title="将代码中所需要的添加进去(一般情况下都添加 . 即所有的意思)"></a>将代码中所需要的添加进去(一般情况下都添加 . 即所有的意思)</h2><pre><code>git add . ||(||或者) git add -A
</code></pre><h2 id="提交"><a href="#提交" class="headerlink" title="提交"></a>提交</h2><pre><code>git commit -m &quot;&lt;1&gt;&quot; 
</code></pre><p><strong>备注 一：此处 引号&lt;&gt;中最好写东西 不写或者 引号内为空 都会上传失败不明白原因</strong></p>
<h2 id="代码拉取和推送"><a href="#代码拉取和推送" class="headerlink" title="代码拉取和推送"></a>代码拉取和推送</h2><blockquote>
<p>   分<strong>两种</strong>情况 </p>
<p> <strong>第一种</strong> 是 没有创建过远程分支 ，简单的操作方式</p>
<p> <strong>第二种</strong> 是 创建了远程分支，这种可以替代第一种 如果没创建远程分支稍显麻烦</p>
</blockquote>
<p> <strong>第一种</strong></p>
<pre><code>git pull  直接拉取 默认的是 master
git push  直接推送 默认的是 master
</code></pre><p> <strong>第二种</strong></p>
<pre><code>git pull origin &lt;本地分支名字&gt;:&lt;远程分支名字&gt;     
</code></pre><p> <strong>备注 二: origin 一般为这个名字 如果本地分支是跟踪远程分支的话可以不写远程分支 直接使用 ：<code>git pull origin &lt;本地分支&gt;</code> 就可以 一般 本地分支和远程分支名字相等</strong></p>
<pre><code>git push origin &lt;本地分支名字&gt;:&lt;远程分支名字&gt;   
</code></pre><p><strong>类比备注 二</strong></p>
<h1 id="分支操作"><a href="#分支操作" class="headerlink" title="分支操作"></a>分支操作</h1><h2 id="查看本地分支"><a href="#查看本地分支" class="headerlink" title="查看本地分支"></a>查看本地分支</h2><pre><code>git branch
</code></pre><h2 id="查看所有分支-包括远程分支"><a href="#查看所有分支-包括远程分支" class="headerlink" title="查看所有分支 包括远程分支"></a>查看所有分支 包括远程分支</h2><pre><code>git branch -a
</code></pre><h2 id="更新分支-比如说你的同事创建了新的分支你却看不到"><a href="#更新分支-比如说你的同事创建了新的分支你却看不到" class="headerlink" title="更新分支(比如说你的同事创建了新的分支你却看不到)"></a>更新分支(比如说你的同事创建了新的分支你却看不到)</h2><pre><code>git fetch
</code></pre><h2 id="创建本地分支"><a href="#创建本地分支" class="headerlink" title="创建本地分支"></a>创建本地分支</h2><pre><code>git branch -b &lt;分支名字&gt;
</code></pre><h2 id="创建远程分支"><a href="#创建远程分支" class="headerlink" title="创建远程分支"></a>创建远程分支</h2><h3 id="第一步先创建一个本地分支"><a href="#第一步先创建一个本地分支" class="headerlink" title="第一步先创建一个本地分支"></a>第一步先创建一个本地分支</h3><pre><code>git branch -b &lt;分支名字&gt;
</code></pre><h3 id="第二步创建远程分支"><a href="#第二步创建远程分支" class="headerlink" title="第二步创建远程分支"></a>第二步创建远程分支</h3><pre><code>git push origin &lt;刚才第一步创建分支的名字&gt;:&lt;最好等于刚才那个分支(远程分支名字)&gt;
</code></pre><h1 id="代码回滚"><a href="#代码回滚" class="headerlink" title="代码回滚"></a>代码回滚</h1><h2 id="保证当前工作区域干净-即代码没有要提交的了并且和远程分支代码一致"><a href="#保证当前工作区域干净-即代码没有要提交的了并且和远程分支代码一致" class="headerlink" title="保证当前工作区域干净(即代码没有要提交的了并且和远程分支代码一致)"></a>保证当前工作区域干净(即代码没有要提交的了并且和远程分支代码一致)</h2><blockquote>
<p>可以将代码提交到远程分支上保证一致性(重复 commit –&gt; pull –&gt; push ) 再 继续</p>
</blockquote>
<h2 id="查看日志"><a href="#查看日志" class="headerlink" title="查看日志"></a>查看日志</h2><pre><code>git reflog
</code></pre><h2 id="找到所需要回滚的版本"><a href="#找到所需要回滚的版本" class="headerlink" title="找到所需要回滚的版本"></a>找到所需要回滚的版本</h2><p><img src="/2016/12/22/Git/Snip20161220_1.png" alt="1"></p>
<blockquote>
<p>比如说 HEAD@{2} 这个是咱们回滚到的版本</p>
</blockquote>
<pre><code>git reset --hard HEAD@{2}
</code></pre><h2 id="删除远程分支"><a href="#删除远程分支" class="headerlink" title="删除远程分支"></a>删除远程分支</h2><pre><code>git push origin :&lt;远程分支名字&gt; 
或者使用 git push origin —delete &lt;远程分支名字&gt;
</code></pre><h2 id="重新创建远程分支"><a href="#重新创建远程分支" class="headerlink" title="重新创建远程分支"></a>重新创建远程分支</h2><pre><code>git push origin &lt;远程分支名字&gt; 
</code></pre><blockquote>
<p>创建新的远程分支 代码就变成了你想要回滚的分支中了</p>
</blockquote>
<h1 id="更新"><a href="#更新" class="headerlink" title="更新"></a>更新</h1><ul>
<li>git init</li>
<li>git add .</li>
<li>git commit -m “xxx”</li>
<li>git remote add origin 网址 (查看 git remote)</li>
<li>git push origin master</li>
<li><a href="http://blog.jobbole.com/87700/" target="_blank" rel="external">引用 一</a></li>
</ul>
]]></content>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>Mac 上搭建Mysql总结</title>
    <url>/2017/01/19/MacMysql/</url>
    <content><![CDATA[<blockquote>
<p>使用<code>Homebrew</code> 搭建<code>Mysql</code>是非常稳妥，因为墙的原因可能速度较慢一些，如果使用梯子可能会很快(比如我)。下面看到我写分号的你也得记得写上。</p>
</blockquote>
<a id="more"></a>
<p> 第一步执行</p>
<pre><code>brew install mysql
</code></pre><p>之后就是等待了建议翻墙速度汇款很多因为内容较大 400多M</p>
<p>启动服务器</p>
<pre><code>base mysql.server start
</code></pre><p> 第二步 进入Mysql</p>
<pre><code>mysql -uroot -p
</code></pre><p>此处会让你输入密码 记得 直接回车！！！直接回车！！！直接回车！！！我在这个地方耽误不少时间</p>
<pre><code>此处应该有一个成功的图片
/-------------------\
</code></pre><p><img src="/2017/01/19/MacMysql/success.png" alt="此处应该有一个成功的图片"></p>
<hr>
<p>查看当前数据库</p>
<pre><code>show databases;    
</code></pre><p><img src="/2017/01/19/MacMysql/show_one.png" alt=""></p>
<p>第三步设置密码<br>此处如果直接设置会提示你选用那个数据库 可以用命令行创建一个自己的数据库不要动它自带的数据库，以免出现错误。</p>
<p>先创建一个新的数据库</p>
<pre><code>create databases DS;
</code></pre><p><img src="/2017/01/19/MacMysql/show_2.png" alt=""></p>
<p>如果不想错的话就把分号加上吧 当然你复制下来最好<code>DS</code>数据库名字随便改别重复就行。。。</p>
<pre><code>use DS;
</code></pre><p>设置密码</p>
<pre><code>set password=password(&apos;xxxxxx&apos;)
</code></pre><p>xxxx即密码</p>
]]></content>
  </entry>
  <entry>
    <title>OC好用的轮子</title>
    <url>/2016/08/19/OC-Goods-Wheels/</url>
    <content><![CDATA[<h2 id="自动处理键盘事件的第三方库"><a href="#自动处理键盘事件的第三方库" class="headerlink" title="自动处理键盘事件的第三方库"></a>自动处理键盘事件的第三方库</h2><h3 id="IQKeyboardManager"><a href="#IQKeyboardManager" class="headerlink" title="IQKeyboardManager"></a>IQKeyboardManager</h3><p><a href="https://github.com/hackiftekhar/IQKeyboardManager" target="_blank" rel="external">IQKeyboardManager</a></p>
<ul>
<li>点击空白处回收键盘</li>
<li>被遮挡上移动控件<h4 id="在AppDelegate中添加"><a href="#在AppDelegate中添加" class="headerlink" title="在AppDelegate中添加"></a>在AppDelegate中添加</h4><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">- (BOOL)application:(UIApplication *)application didFinishLaunchingWithOptions:(NSDictionary *)launchOptions &#123;</div><div class="line">    </div><div class="line">    IQKeyboardManager *manager = [IQKeyboardManager sharedManager];</div><div class="line">    manager.enable = YES;</div><div class="line">    manager.shouldResignOnTouchOutside = YES;</div><div class="line">    manager.shouldToolbarUsesTextFieldTintColor = YES;</div><div class="line">    manager.enableAutoToolbar = NO;</div><div class="line">    </div><div class="line">    return YES;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
</li>
</ul>
<a id="more"></a>
<h2 id="Masonry-代码约束-这个我一直在用，感觉就是神器"><a href="#Masonry-代码约束-这个我一直在用，感觉就是神器" class="headerlink" title="Masonry 代码约束(这个我一直在用，感觉就是神器)"></a>Masonry 代码约束(这个我一直在用，感觉就是神器)</h2><p><a href="https://github.com/SnapKit/Masonry" target="_blank" rel="external">Masonry</a></p>
<h2 id="未完待续"><a href="#未完待续" class="headerlink" title="未完待续"></a>未完待续</h2>]]></content>
      <tags>
        <tag>iOS</tag>
      </tags>
  </entry>
  <entry>
    <title>iOS内购买IAP和服务器交互问题</title>
    <url>/2016/09/08/OC-IAP/</url>
    <content><![CDATA[<blockquote>
<p>#warning 此处不讲解IA如何P配置了，网上挺全面的而且还写的都不错的。但是前提是你有沙盒测试账号和itunesconnect设置过了内购买所需虚拟物品和银行协议的一堆配置<br>此处只讲解两点</p>
<ul>
<li>我碰见的问题</li>
<li>和服务器的交互问题(此处以PHP为例子，本人PHP刚入门也是copy来的，根据自己的需求做出小小改变)</li>
</ul>
</blockquote>
<a id="more"></a>
<h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><h3 id="配置消耗型物品重复购买却显示此项目免费恢复"><a href="#配置消耗型物品重复购买却显示此项目免费恢复" class="headerlink" title="配置消耗型物品重复购买却显示此项目免费恢复"></a>配置消耗型物品重复购买却显示此项目免费恢复</h3><ul>
<li><p>需要在</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">- (void)paymentQueue:(SKPaymentQueue *)queue updatedTransactions:(NSArray&lt;SKPaymentTransaction *&gt; *)transactions</div></pre></td></tr></table></figure>
</li>
<li><p>的每个状态后面都需要添加</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">[[SKPaymentQueue defaultQueue] finishTransaction:tran];</div></pre></td></tr></table></figure>
</li>
</ul>
<h2 id="交互"><a href="#交互" class="headerlink" title="交互"></a>交互</h2><h3 id="先解释一下OC代码和PHP代码"><a href="#先解释一下OC代码和PHP代码" class="headerlink" title="先解释一下OC代码和PHP代码"></a>先解释一下OC代码和PHP代码</h3><blockquote>
<p>第一步拾掇拾掇需要的东西</p>
<ul>
<li>#import <storekit storekit.h=""></storekit></li>
<li><skpaymenttransactionobserver, skproductsrequestdelegate=""> 两个协议方法你也得实现吧<br>第二步注册观察者，并且判断该用户能否使用内购买<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">[[SKPaymentQueue defaultQueue] addTransactionObserver:self];</div><div class="line">            if([SKPaymentQueue canMakePayments])&#123;</div><div class="line">                weakSelf.currentProId         = productID;</div><div class="line">                [weakSelf requestProductData:productID];</div><div class="line">            &#125;else&#123;</div><div class="line">                UIAlertView *alerView         = [[UIAlertView alloc] initWithTitle:@&quot;提示&quot;</div><div class="line">                                                                           message:@&quot;您的手机没有打开程序内付费购买&quot;</div><div class="line">                                                                          delegate:self cancelButtonTitle:NSLocalizedString(@&quot;关闭&quot;,nil) otherButtonTitles:NSLocalizedString(@&quot;提示&quot;,nil), nil];     </div><div class="line">                [alerView show];</div><div class="line">            &#125;</div></pre></td></tr></table></figure>
</skpaymenttransactionobserver,></li>
</ul>
<p>第三步查询 =&gt; 你传一个你在itunesconnect 中App中定义的内购买Product_ID<br><img src="/2016/09/08/OC-IAP/itunesconntent.png" alt="我是图"><br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">NSArray *product = [[NSArray alloc] initWithObjects:type, nil];</div><div class="line">NSSet *nsset = [NSSet setWithArray:product];</div><div class="line">// 请求动作</div><div class="line">SKProductsRequest *request = [[SKProductsRequest alloc] initWithProductIdentifiers:nsset];</div><div class="line">request.delegate = self;</div><div class="line">[request start];</div></pre></td></tr></table></figure></p>
<p>第四步 收到你在itunesconnect 中定义的Product_ID的详细数据,顺便把购买请求发送了()<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">- (void)productsRequest:(SKProductsRequest *)request didReceiveResponse:(SKProductsResponse *)response &#123;</div><div class="line">    NSLog(@&quot;--------------收到产品反馈消息---------------------&quot;);</div><div class="line">    NSArray *product              = response.products;</div><div class="line">    if([product count] == 0)&#123;</div><div class="line">        //[SVProgressHUD dismiss];</div><div class="line">        NSLog(@&quot;--------------没有商品------------------&quot;);</div><div class="line">        return;</div><div class="line">    &#125;</div><div class="line">    </div><div class="line">    NSLog(@&quot;productID:%@&quot;, response.invalidProductIdentifiers);</div><div class="line">    NSLog(@&quot;产品付费数量:%lu&quot;,(unsigned long)[product count]);</div><div class="line">    </div><div class="line">    SKProduct *p                  = nil;</div><div class="line">    for (SKProduct *pro in product) &#123;</div><div class="line">        NSLog(@&quot;%@&quot;, [pro description]);</div><div class="line">        NSLog(@&quot;%@&quot;, [pro localizedTitle]);</div><div class="line">        NSLog(@&quot;%@&quot;, [pro localizedDescription]);</div><div class="line">        NSLog(@&quot;%@&quot;, [pro price]);</div><div class="line">        NSLog(@&quot;%@&quot;, [pro productIdentifier]);</div><div class="line">        </div><div class="line">        if([pro.productIdentifier isEqualToString:_currentProId])&#123;</div><div class="line">            p                             = pro;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    </div><div class="line">    SKPayment *payment            = [SKPayment paymentWithProduct:p];</div><div class="line">    NSLog(@&quot;发送购买请求&quot;);</div><div class="line">    [[SKPaymentQueue defaultQueue] addPayment:payment];</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>第五步收到信息给你返回数据了<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">- (void)paymentQueue:(SKPaymentQueue *)queue updatedTransactions:(NSArray&lt;SKPaymentTransaction *&gt; *)transactions &#123;</div><div class="line">    for(SKPaymentTransaction *tran in transactions)&#123;</div><div class="line">        </div><div class="line">        switch (tran.transactionState) &#123;</div><div class="line">            case SKPaymentTransactionStatePurchased:&#123;</div><div class="line">                NSLog(@&quot;交易完成&quot;);</div><div class="line">                //方法在下面</div><div class="line">                [self completeTransaction:tran];</div><div class="line">            &#125;</div><div class="line">                break;</div><div class="line">            case SKPaymentTransactionStatePurchasing:</div><div class="line">                NSLog(@&quot;商品添加进列表&quot;);</div><div class="line">                </div><div class="line">                break;</div><div class="line">            case SKPaymentTransactionStateRestored:&#123;</div><div class="line">                NSLog(@&quot;已经购买过商品&quot;);</div><div class="line">                </div><div class="line">                [[SKPaymentQueue defaultQueue] finishTransaction:tran];</div><div class="line">            &#125;</div><div class="line">                break;</div><div class="line">            case SKPaymentTransactionStateFailed:&#123;</div><div class="line">                NSLog(@&quot;交易失败&quot;);</div><div class="line">                [[SKPaymentQueue defaultQueue] finishTransaction:tran];</div><div class="line">              //  [SVProgressHUD showErrorWithStatus:@&quot;购买失败&quot;];</div><div class="line">                startConntentService = false;</div><div class="line">            &#125;</div><div class="line">                break;</div><div class="line">            default:</div><div class="line">                break;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>此处是最后的方法还有我和服务器交互的地方，但是此处我也不懂为什么这个方法会调用两次如果您明白，请在下面留言告诉我谢谢，所以我此处加了一个全局的判断，来约束它和服务器交互的次数。<br>！！！看好我发送数据请求的方法其实就是AFNetworing的封装，都会用对吧。。。网址看好，别光顾着复制哦<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">- (void)completeTransaction:(SKPaymentTransaction *)transaction &#123;</div><div class="line">    NSLog(@&quot;交易结束&quot;);</div><div class="line">    [SVProgressHUD dismiss];</div><div class="line">    </div><div class="line">    NSURL *receiptURL             = [[NSBundle mainBundle] appStoreReceiptURL];</div><div class="line">    NSData *receipt               = [NSData dataWithContentsOfURL:receiptURL];</div><div class="line">    if (!receipt) &#123; /* No local receipt -- handle the error. */ &#125;</div><div class="line">    //因为此处可能会多次调用原因不明所以加判断只调用一次</div><div class="line">    else if (receipt &amp;&amp; startConntentService) &#123;</div><div class="line">        /**</div><div class="line">                  服务器要做的事情:</div><div class="line">                  接收ios端发过来的购买凭证。</div><div class="line">                  判断凭证是否已经存在或验证过，然后存储该凭证。</div><div class="line">                  将该凭证发送到苹果的服务器验证，并将验证结果返回给客户端。</div><div class="line">                  如果需要，修改用户相应的会员权限</div><div class="line">                 */</div><div class="line">        startConntentService = false;</div><div class="line">        //字典中第二个参数是为了debug准备的，正常你不用写</div><div class="line">        NSDictionary *requestContents = @&#123;</div><div class="line">                                          @&quot;receipt-data&quot;: [receipt base64EncodedStringWithOptions:0],</div><div class="line">                                          @&quot;XDEBUG_SESSION_START&quot;:@&quot;12477&quot;</div><div class="line">                                          &#125;;</div><div class="line">        [HTTPClient postWithURLString:@&quot;你的后台网址&quot; parameters:requestContents success:^(id returnValue) &#123;</div><div class="line">            id name                       = returnValue;</div><div class="line">            </div><div class="line">        &#125; failure:^(id failureValue) &#123;</div><div class="line">            </div><div class="line">        &#125;];</div><div class="line">    &#125;</div><div class="line">    //这个千万别忘了 ，要不你就会犯第一条问题(配置消耗型物品重复购买却显示此项目免费恢复)</div><div class="line">    [[SKPaymentQueue defaultQueue] finishTransaction:transaction];</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
</blockquote>
<h3 id="下面是PHP代码，我用的是ThinkPHP框架-会php都会框架，像我只会框架暂时还不会php-咳咳…"><a href="#下面是PHP代码，我用的是ThinkPHP框架-会php都会框架，像我只会框架暂时还不会php-咳咳…" class="headerlink" title="下面是PHP代码，我用的是ThinkPHP框架(会php都会框架，像我只会框架暂时还不会php.咳咳…)"></a>下面是PHP代码，我用的是ThinkPHP框架(会php都会框架，像我只会框架暂时还不会php.咳咳…)</h3><blockquote>
<p>只有两个方法<br>1 外部调用的方法，此处我的逻辑都系在了Controller中，是为了大家方便，我建议还是写在Model中(MVC哦，其实感觉都一样哈哈)<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">public function iosIAPPay() &#123;</div><div class="line"></div><div class="line">        $status = array(&apos;status&apos;=&gt;-1);</div><div class="line">        //获取 App 发送过来的数据,设置时候是沙盒状态</div><div class="line">        $receipt   = $_POST[&apos;receipt-data&apos;];</div><div class="line">        $isSandbox = true;</div><div class="line">        //开始执行验证</div><div class="line">        try</div><div class="line">        &#123;</div><div class="line">            $info = $this-&gt;getReceiptData($receipt, $isSandbox);</div><div class="line">            // 通过product_id 来判断是下载哪个资源</div><div class="line">            switch($info[&apos;product_id&apos;])&#123;</div><div class="line">                case &apos;你的Product_ID&apos;:</div><div class="line">                    $status[&apos;status&apos;] = 1;</div><div class="line">                    Header(&quot;Location:xxxx.zip&quot;);</div><div class="line">                    break;</div><div class="line">            &#125;</div><div class="line">            return $status;</div><div class="line">        &#125;</div><div class="line">            //捕获异常</div><div class="line">        catch(\Exception $e)</div><div class="line">        &#123;</div><div class="line">            $this-&gt;ajaxReturn($status);</div><div class="line">        &#125;</div><div class="line">    &#125;</div></pre></td></tr></table></figure></p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">//服务器二次验证代码</div><div class="line">    function getReceiptData($receipt, $isSandbox = false)</div><div class="line">    &#123;</div><div class="line">        if ($isSandbox) &#123;</div><div class="line">            $endpoint = &apos;https://sandbox.itunes.apple.com/verifyReceipt&apos;;</div><div class="line">        &#125;</div><div class="line">        else &#123;</div><div class="line">            $endpoint = &apos;https://buy.itunes.apple.com/verifyReceipt&apos;;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        $postData = json_encode(array(&quot;receipt-data&quot; =&gt; $receipt));;</div><div class="line"></div><div class="line">        $ch = curl_init($endpoint);</div><div class="line">        curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);</div><div class="line">        curl_setopt($ch, CURLOPT_POST, true);</div><div class="line">        curl_setopt($ch, CURLOPT_POSTFIELDS, $postData);</div><div class="line">        curl_setopt ($ch, CURLOPT_SSL_VERIFYPEER, 0);  //这两行一定要加，不加会报SSL 错误</div><div class="line">        curl_setopt ($ch, CURLOPT_SSL_VERIFYHOST, 0);</div><div class="line"></div><div class="line"></div><div class="line">        $response = curl_exec($ch);</div><div class="line">        $errno    = curl_errno($ch);</div><div class="line">        $errmsg   = curl_error($ch);</div><div class="line">        curl_close($ch);</div><div class="line">        //判断时候出错，抛出异常</div><div class="line">        if ($errno != 0) &#123;</div><div class="line">            throw new \Exception($errmsg, $errno);</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        $data = json_decode($response);</div><div class="line">        //此处是看到先人们的指导，又看到apple的官方说法改的。否则会审核不过貌似是审核也会走沙盒测试者，</div><div class="line">        //此处先判断一次返回的status是否=21007 这数据是从测试环境，但它发送到生产环境中进行验证。它发送到测试环境来代替。</div><div class="line">        if ($data-&gt;status == 21007) &#123;</div><div class="line">            $this-&gt;getReceiptData($receipt,true);</div><div class="line">            return;</div><div class="line">        &#125;</div><div class="line">        //判断返回的数据是否是对象</div><div class="line">        if (!is_object($data)) &#123;</div><div class="line">            throw new \Exception(&apos;Invalid response data&apos;);</div><div class="line">        &#125;</div><div class="line">        //判断购买时候成功</div><div class="line">        if (!isset($data-&gt;status) || $data-&gt;status != 0) &#123;</div><div class="line">            throw new \Exception(&apos;Invalid receipt&apos;);</div><div class="line">        &#125;</div><div class="line">         $in_app = $data-&gt;receipt-&gt;in_app;</div><div class="line">        //返回产品的信息</div><div class="line">        </div><div class="line">         $status[&apos;data&apos;] = array(</div><div class="line">            &apos;quantity&apos; =&gt; $in_app-&gt;quantity,</div><div class="line">            &apos;product_id&apos; =&gt; $in_app-&gt;product_id,</div><div class="line">            &apos;transaction_id&apos; =&gt; $in_app-&gt;transaction_id,</div><div class="line">            &apos;purchase_date&apos; =&gt; $in_app-&gt;purchase_date,</div><div class="line">            &apos;app_item_id&apos; =&gt; $data-&gt;receipt-&gt;app_item_id,</div><div class="line">        );</div><div class="line">        return(</div><div class="line">            $status;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<blockquote>
<p>我一直都困在和后台交互的时候苹果返回的数据中status=21002,哭死。。。因为status=0才是购买成功.</p>
<ul>
<li>OC中将苹果所需的凭证其实就是<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">NSData *receipt               = [NSData dataWithContentsOfURL:receiptURL];</div></pre></td></tr></table></figure>
</li>
</ul>
<p>base64EncodedStringWithOptions一次<br>在PHP在将这个数据json_encode(array(“receipt-data” =&gt; $receipt));一次在传给苹果服务器，自己写百度谷歌一起来真心好难。<br>一遍一遍的改OC和PHP代码，还好公司太小两个都是我在做，我有大把时间和不用麻烦别人。</p>
<h3 id="总结一下"><a href="#总结一下" class="headerlink" title="总结一下"></a>总结一下</h3><p>帮大家理理思路也是我目前知道的最好的app和服务器交互的方法：<br>在支付之前在后台服务器记录一次你的数据，包括product_id，之后才开始去支付支付成功后再和服务器进行比对product_id可以预防app本地破解支付和防止用户篡改(比如他买的518却只支付6块)等.<br>还有就是漏单问题，就是用户买东西成功了，但是和自己服务器交互出现了意外(不会怀孕)，我现在并未尝试但觉得可行(在更新这个博客之前，因为今天周五要回家了所以下周测试)，就是讲xcode的凭证保存到本地，这样用户就可以自己重新点击一次，在和服务器交互一次，将虚拟物品重新不给他一次。</p>
</blockquote>
<p>参考:</p>
<blockquote>
<p><a href="http://www.jianshu.com/p/e9ae4cece800" target="_blank" rel="external">应用内支付IAP全部流程</a><br><a href="http://www.jianshu.com/p/86ac7d3b593a" target="_blank" rel="external">iOS开发内购全套图文教程</a></p>
</blockquote>
]]></content>
      <categories>
        <category>iOS功能</category>
      </categories>
      <tags>
        <tag>iOS</tag>
      </tags>
  </entry>
  <entry>
    <title>学习PHP遇到的问题</title>
    <url>/2016/08/09/PHP-Study-problems/</url>
    <content><![CDATA[<h2 id="json-decode出现的怀疑人生问题"><a href="#json-decode出现的怀疑人生问题" class="headerlink" title="json_decode出现的怀疑人生问题"></a>json_decode出现的怀疑人生问题</h2><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">$respBody = &apos;&#123;&quot;message&quot;:&quot;streamStatus&quot;,&quot;updatedAt&quot;:&quot;2016-08-09T10:20:38.035549157+08:00&quot;,&quot;data&quot;:&#123;&quot;id&quot;:&quot;z1.com-jyjapp-zb.JYJLive414&quot;,&quot;url&quot;:&quot;rtmp://127.0.0.1/com-jyjapp-zb/JYJLive414?key=4846e65a-cce6-4e62-a375-745d7f02</div><div class="line">        e032\u0026sourceConnId=zOmKiH2XRsuWmWht\u0026node=vc-scgy-tel-1-1\u0026remote=171.217.118.64%3A51789&quot;,&quot;status&quot;:&quot;connected&quot;&#125;&#125;&apos;;</div><div class="line">$re  = json_decode($respBody,true);</div></pre></td></tr></table></figure>
<p>这一串很普通的代码$respBody = null解析失败的原因居然是—–&gt;不能换行的字符<br>你会问我这么简单的问题看不出来吗中间明显空了一块吗！但是在编辑器中是分辨不出来的</p>
]]></content>
  </entry>
  <entry>
    <title>Swift-数组&amp;链表</title>
    <url>/2020/05/23/swift_algorithm_0/</url>
    <content><![CDATA[<blockquote>
<p>数组和链表都是线性数据结构</p>
</blockquote>
<a id="more"></a>
<h2 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h2><blockquote>
<p>数组插入和删除，都要重新移动位置内存位置要重新布局，相较于它的查询较慢. </p>
<p>插入和删除时间复杂度O(N), 如果很幸运的插入到最后面复杂度是O(1), 但是插入到最前面则是O(N), 平均是O(N/2)还是O(N)</p>
<p>查询时间复杂度O(1)</p>
</blockquote>
<h2 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h2><blockquote>
<p>33444</p>
</blockquote>
<p><img src="/2020/05/23/swift_algorithm_0/node_head.png" alt="333"></p>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
  </entry>
  <entry>
    <title>微信小程序问题总结</title>
    <url>/2016/10/02/WX-IDE/</url>
    <content><![CDATA[<blockquote>
<p>我是做iOS开发的，很多人都会说这个微信小程序又是动态的，我不反对苹果对动态的讨厌，毕竟这样很容易不受控制，但是<a href="http://reactnative.cn/" target="_blank" rel="external">ReactNative(中文网)</a>不是还活的好好的吗(貌似版本到了0.33了)!而且我觉得可以学个大概万一真用到的时候也不至于无所适从(我先做个非常简陋的电商类，完事后放到下面)。学习！学习！学习！才不会被淘汰。</p>
<p>申明 ：会将我认为是问题的问题记录下来，如果您觉得太简单可以关掉。并且我是边做小程序边写的这个博客，会一直更新，！！！MAC环境</p>
<p>我可以说不太懂微信小程序的语言。它使用XML+JS ，不支持HTML5，兼容部分CSS写法(从这个句号到这句话完事我是抄来的，因为我不懂XML和JS)。不懂但是不能开发吗？我觉得语言都是相通的，还好之前做了<code>React Native</code>项目(总觉得很相似)，上手小程序也很快。</p>
<p>ps: 很冷清。。。我知道我是菜鸟</p>
</blockquote>
<a id="more"></a>
<h2 id="从头开始-开始一个项目"><a href="#从头开始-开始一个项目" class="headerlink" title="从头开始 - 开始一个项目"></a>从头开始 - 开始一个项目</h2><h3 id="工具下载安装"><a href="#工具下载安装" class="headerlink" title="工具下载安装"></a>工具下载安装</h3><p><a href="https://servicewechat.com/wxa-dev-logic/download_redirect?type=darwin&amp;from=mpwiki&amp;t=1475052055457" target="_blank" rel="external">微信小程序开发者工具下载</a></p>
<ul>
<li>开发工具缺少appid问题，这个只要破解就可以了(<a href="http://blog.csdn.net/chenqiuge1984/article/details/52692394" target="_blank" rel="external">引用 一</a>) </li>
</ul>
<h3 id="新建项目"><a href="#新建项目" class="headerlink" title="新建项目"></a>新建项目</h3><ul>
<li>不清楚是开发工具的原因还是怎么回事，说好新建一个项目会有一个QuickStart的，然而我并没有所以打开就是这个样子的</li>
</ul>
<p><img src="/2016/10/02/WX-IDE/startError.png" alt="11"></p>
<ul>
<li>只能从官网下载一个<a href="https://mp.weixin.qq.com/debug/wxadoc/dev/demo/quickstart.zip?t=1475052047016" target="_blank" rel="external">HelloWorld</a>直接放大开采创建的项目路径下面</li>
</ul>
<h3 id="文件结构"><a href="#文件结构" class="headerlink" title="文件结构"></a>文件结构</h3><p><img src="/2016/10/02/WX-IDE/appDetail.png" alt="默认的"><br>默认就几个文件剩下的就是目录再加上不同名字的这个几个文件当然还得加上.wxml，这个文件就是来书写界面的</p>
<h2 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h2><p>app.json</p>
<p><img src="/2016/10/02/WX-IDE/appJson.png" alt=""></p>
<ul>
<li>pages : 应该就是项目加载文件的入口(用应该的原因是我不确定)，如果此处不加载的话后面用了也是不会有所显示的(这里是确定的如果不信可以尝试)。</li>
<li>window : 程序的窗口</li>
<li>tabBar : 很好理解就是标签栏(App下面负责切换不同界面)</li>
</ul>
<h2 id="需记录问题"><a href="#需记录问题" class="headerlink" title="需记录问题"></a>需记录问题</h2><h3 id="scroll-view"><a href="#scroll-view" class="headerlink" title="scroll-view"></a>scroll-view</h3><h4 id="注意问题"><a href="#注意问题" class="headerlink" title="注意问题"></a>注意问题</h4><ul>
<li>横向需要给scroll-view 一个高度<code>height: 200px</code>文档上是这么说的但是scroll-view中元素设置高度就可以将scroll-view撑起来了</li>
</ul>
<h4 id="只展示第一个元素滑动无效"><a href="#只展示第一个元素滑动无效" class="headerlink" title="只展示第一个元素滑动无效"></a>只展示第一个元素滑动无效</h4><ul>
<li><p>scroll-view需要设置<code>white-space: nowrap;</code>(百度一下:文本不会换行，文本会在在同一行上继续，直到遇到 <br> 标签为止。)</p>
</li>
<li><p>子元素设置<code>display:inline-block</code>(百度一下:让一个元素具有“区块元素”的属性（可以设置width和height），又具有“内联元素”的属性（不产生换行))</p>
</li>
</ul>
<p>这样scroll-view可以正常展示了</p>
<blockquote>
<p>问题：但是scroll-view如果绑定<code>wx:for = &#39;&#39;</code>子类中有一个view 他一直都是<code>column</code>排列不会是<code>row</code>排列。希望有人帮解决</p>
</blockquote>
<p>针对上面问题 我只能用<code>swiper</code>来解决暂时未想到办法.</p>
<p>发布于2016-10-2日 </p>
<p><a href="https://github.com/DSPerson/wx-demo" target="_blank" rel="external">自己做的demo很丑但是希望给星</a></p>
<p><a href="http://blog.csdn.net/chenqiuge1984/article/details/52692394" target="_blank" rel="external">引用 一</a></p>
]]></content>
      <categories>
        <category>微信小程序</category>
      </categories>
  </entry>
  <entry>
    <title>lldb 学习</title>
    <url>/2017/06/14/lldb%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<blockquote>
<p>lldb 替代gdb 在程序运行中 暂停 即可使用lldb了 在项目中可以有多帮助</p>
</blockquote>
<a id="more"></a>
<h1 id="安装Chisel"><a href="#安装Chisel" class="headerlink" title="安装Chisel"></a>安装Chisel</h1><p><a href="https://github.com/facebook/chisel" target="_blank" rel="external">Chisel网址</a></p>
<p>也可以 在终端直接</p>
<pre><code>brew update
brew install chisel
</code></pre><p><img src="/2017/06/14/lldb学习/Snip20170614_1.png" alt=""></p>
<blockquote>
<p>成功后 将红色框内在 终端 中 输入 <code>touch ~/.lldbinit</code> 复制进去 重启Xcode</p>
</blockquote>
<h2 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h2><blockquote>
<p>都是在项目在运行后 暂停条件下</p>
</blockquote>
<p> 使用 <code>visualize</code> + 内存地址即可在预览中打开图片查看</p>
<p> 使用 <code>pvc</code> 查看当前NavigationBar栈</p>
<p> 使用 <code>pintnals</code> + 内存地址 查看 私有属性</p>
<p> 使用 <code>pviews</code> 从当前到当前View所有视图 <code>pviews</code>  + 内存地址</p>
<p> 使用 <code>caflush</code> 手动刷新</p>
<p> 使用 <code>presponder + 内存地址</code> 查看当前地址的层级</p>
<p> 使用 <code>taplog</code> 会自动关闭暂停之后点击任意位置 会打印出最近一个     响应点击时间的View</p>
<p> 使用 <code>pclass + 内存地址</code> 查看继承关系</p>
<p> 使用 <code>bmessage + 类 + 方法</code> 类似断点</p>
<p> 使用 <code>show</code> 或者 <code>hide</code> + 内存地址 来 展示或者隐藏控件</p>
]]></content>
      <categories>
        <category>iOS功能</category>
      </categories>
      <tags>
        <tag>iOS</tag>
      </tags>
  </entry>
  <entry>
    <title>回首2016，展望2017</title>
    <url>/2017/02/04/talkIsCheap/</url>
    <content><![CDATA[<p>2016年是iOS 市场崩溃的一年，15年踏入这个行列中，在16年差点被挤死，还好我挺了过来。</p>
<a id="more"></a>
<p>我在我第一家公司呆了一年多，没想到(也不能说想到，只不过不想去想)我成功了把它干黄了，也是挺骄傲的，毕竟挺不容易的，但是欠我的两个月工资还没给，估计就得17年仲裁了，17年第一件展望的事情出来了。</p>
<p>16年学到了很多，ReactNative火了一把，我也跟着蹭了一把。用PHP学过后台，也算走到了IT领域的后方(可惜不太合格)。</p>
<p>17年展望,也算是16年给自己找了个奔头，不知道能不能成功，自己做个小项目看看能不能挖到传说中的第一桶金，应该是和我女朋友配合一下共同完成，但是截至目前为止是我一个人在搭建后台，等差不多了在开始App方向的搭建。</p>
<p>于2017年2月4日 —&gt; 第一桶金来娶我女朋友</p>
]]></content>
      <categories>
        <category>闲聊</category>
      </categories>
  </entry>
  <entry>
    <title>面试总结</title>
    <url>/2017/06/13/%E9%9D%A2%E8%AF%95/</url>
    <content><![CDATA[<blockquote>
<p>被面试问的问题, 和网上找到的我觉得不错的问题</p>
<p>会保持更新 – 因都写在一起了, 所以可能会穿插添加. 请谅解</p>
</blockquote>
<a id="more"></a>
<h1 id="iOS-多线程"><a href="#iOS-多线程" class="headerlink" title="iOS-多线程"></a>iOS-多线程</h1><h2 id="常见的几种线程锁-保证线程安全"><a href="#常见的几种线程锁-保证线程安全" class="headerlink" title="常见的几种线程锁 / 保证线程安全"></a>常见的几种线程锁 / 保证线程安全</h2><ul>
<li>@synchronized 适用线程不多,任务量不大的多线程加锁</li>
<li>NSLock 常用锁 性能一般</li>
<li>dispatch_semaphore_t GCD信号锁,性能不错</li>
<li>OSSpinLock 性能非常高 但是线程出现了问题</li>
</ul>
<h1 id="Run字辈"><a href="#Run字辈" class="headerlink" title="Run字辈"></a>Run字辈</h1><h2 id="简单讲一下Runtime"><a href="#简单讲一下Runtime" class="headerlink" title="简单讲一下Runtime"></a>简单讲一下Runtime</h2><blockquote>
<p>Runtime 它是OC的运行时, 其中最重要的就是消息机制 C语言是编译时就已经确定了如何调用.<br>OC是在运行的时候才会确定</p>
</blockquote>
<h3 id="当我们调用了一个方法的时候-运行过程是"><a href="#当我们调用了一个方法的时候-运行过程是" class="headerlink" title="当我们调用了一个方法的时候 运行过程是?"></a>当我们调用了一个方法的时候 运行过程是?</h3><ol>
<li>Runtime会将我们的方法调用 转为消息发送<code>objc_msgSend</code>, 并且把方法的调用者和方法选择器 作为参数一起传递过去</li>
<li>方法的调用者会通过isa指针查找到该方法所属的类, 然后在Cache 或者 Method_Lists中查找该方法, 如果有则调用执行</li>
<li>如果没有找到该方法, 则通过SuperClass 继续像父类查找(如何到NSObject还未查找到则进入消息转发)</li>
</ol>
<h3 id="动态方法解析"><a href="#动态方法解析" class="headerlink" title="动态方法解析"></a>动态方法解析</h3><blockquote>
<p>一般情况下程序Carsh 类似</p>
<p><code>unrecognized selector sent to instance 内存地址</code></p>
<p>在Crash之前Runtime会给我们补救的机会(消息转发) </p>
</blockquote>
<ol>
<li>检测这个 selector 是不是要忽略的。比如 Mac OS X 开发，有了垃圾回收就不理会 retain，release 这些函数了</li>
<li>检测这个 target 是不是 nil 对象。ObjC 的特性是允许对一个 nil 对象执行任何一个方法不会 Crash，因为会被忽略掉</li>
<li>如果上面两个都过了，那就开始查找这个类的 IMP，先从 cache 里面找，完了找得到就跳到对应的函数去执行. 如果 cache 找不到就找一下方法分发表</li>
<li>如果分发表找不到就到超类的分发表去找，一直找，直到找到NSObject类为止</li>
</ol>
<blockquote>
<p>如果还找不到就要开始进入消息转发了，消息转发的大致过程如图：</p>
</blockquote>
<p><img src="http://cc.cocimg.com/api/uploads/20160520/1463741650158770.jpg" alt=""></p>
<ol>
<li>进入<code>resolveInstanceMethod:</code>方法,询问是否动态添加方法,如果返回Yes, 通过<code>class_addMethod</code>动态添加函数方法, 消息的处理完毕, 此流程完毕. 返回No 进入 2</li>
<li>进入<code>ForwardTargetForSelector:</code> 方法, 用于指定那个对象来响应这个<code>Selector</code>, 如果返回某个对象, 该对象调用此方法, 此流程完毕. 返回nil 进入 3</li>
<li>通过<code>methodSignatureForSelector:</code>来指定方法签名,返回nil, 表示不处理, 返回签名(通过<code>NSMethodSignature signatureWithObjcType:&quot;v@:&quot;</code> 来进行重新签名)则进入 4</li>
<li>调用<code>forwardInvlication:</code>方法, 通过<code>invocation</code> 修改实现方法，修改响应对象等。</li>
</ol>
<h3 id="Runtime-能做什么"><a href="#Runtime-能做什么" class="headerlink" title="Runtime 能做什么"></a>Runtime 能做什么</h3><ul>
<li>字典和模型相互转换</li>
<li>方法交换</li>
<li>给分类添加属性</li>
<li>自动归解档</li>
</ul>
<h3 id="Category-原理"><a href="#Category-原理" class="headerlink" title="Category 原理"></a>Category 原理</h3><p><a href="http://blog.leichunfeng.com/blog/2015/05/18/objective-c-category-implementation-principle/" target="_blank" rel="external">参考自</a></p>
<blockquote>
<p>通过<code>attachCategoryMethods</code>这个函数来处理的, 将类中旧方法和Category新添加的方法整合成一个新的方法列表, 并赋值给<code>method_lists</code> 或者 <code>method_list</code>. 主类中的方法和 Category 中的方法在 runtime 看来并没有区别，它们是被同等对待的，都保存在主类的方法列表中。我们需要调用时引入头文件, 是要”照顾”编辑器大大的感受而已…</p>
</blockquote>
<h3 id="Runtime-常见的定义"><a href="#Runtime-常见的定义" class="headerlink" title="Runtime 常见的定义"></a>Runtime 常见的定义</h3><ul>
<li>IMP implementation 实现的意思 指向一个方法实现的指针</li>
</ul>
<h2 id="Runloop"><a href="#Runloop" class="headerlink" title="Runloop"></a>Runloop</h2><blockquote>
<p>一般来讲，一个线程一次只能执行一个任务，执行完成后线程就会退出。如果我们需要一个机制，让线程能随时处理事件但并不退出</p>
</blockquote>
<p><a href="http://blog.ibireme.com/2015/05/18/runloop/" target="_blank" rel="external">超详细Runloop</a></p>
<h3 id="NSTime-为什么在滑动UIScrollView时候会暂停"><a href="#NSTime-为什么在滑动UIScrollView时候会暂停" class="headerlink" title="NSTime 为什么在滑动UIScrollView时候会暂停"></a>NSTime 为什么在滑动UIScrollView时候会暂停</h3><blockquote>
<p>因为Runloop 原因, 为了保证UIScrollView滑动时候的流畅度, 会将Runloop的Mode切换为<code>TrackingRunLoopMode</code>, 这时候NSTimer就不会得到回调. 只需要将NSTimer的model 改为 <code>commonModeItems</code>即可了</p>
</blockquote>
<h2 id="NSAutoreleasePool"><a href="#NSAutoreleasePool" class="headerlink" title="NSAutoreleasePool"></a>NSAutoreleasePool</h2><blockquote>
<p> NSAutoreleasePool 类被用来支持自动引用计数内存管理系统.</p>
</blockquote>
<h3 id="autoreleased-对象什么时候释放"><a href="#autoreleased-对象什么时候释放" class="headerlink" title="autoreleased 对象什么时候释放"></a>autoreleased 对象什么时候释放</h3><blockquote>
<p>对象创建后会被添加到最近的<code>autoReleasePool</code>中, 只有这个<code>autoReleasePool</code> 自身drain的时候, <code>autoReleasePool</code>中的对象才会被<code>release</code></p>
<p>根据苹果官方文档中对 NSAutoreleasePool 的描述，我们可知，在主线程的 NSRunLoop 对象（在系统级别的其他线程中应该也是如此，比如通过 <code>dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, 0)</code> 获取到的线程）的每个 event loop 开始前，系统会自动创建一个 autoreleasepool ，并在 event loop 结束时 drain 。我们上面提到的场景 1 中创建的 autoreleased 对象就是被系统添加到了这个自动创建的 autoreleasepool 中，并在这个 autoreleasepool 被 drain 时得到释放</p>
</blockquote>
<h3 id="AutoreleasePoolPage"><a href="#AutoreleasePoolPage" class="headerlink" title="AutoreleasePoolPage"></a>AutoreleasePoolPage</h3><blockquote>
<p>那这里的 AutoreleasePoolPage 是什么东西呢？其实，autoreleasepool 是没有单独的内存结构的，它是通过以 AutoreleasePoolPage 为结点的双向链表来实现的。<br> autoreleasepool 的运行过程可以简单地理解为 <code>objc_autoreleasePoolPush()</code>、<code>[对象 autorelease]</code> 和 <code>objc_autoreleasePoolPop(void *)</code> 三个过程</p>
</blockquote>
<h3 id="和线程之间的关系"><a href="#和线程之间的关系" class="headerlink" title="和线程之间的关系"></a>和线程之间的关系</h3><blockquote>
<p>每一个线程都会维护自己的 autoreleasepool 堆栈。换句话说 autoreleasepool 是与线程紧密相关的，每一个 autoreleasepool 只对应一个线程。</p>
</blockquote>
<h1 id="iOS手势问题"><a href="#iOS手势问题" class="headerlink" title="iOS手势问题"></a>iOS手势问题</h1><h2 id="响应者链"><a href="#响应者链" class="headerlink" title="响应者链"></a>响应者链</h2><blockquote>
<p>处理事件响应者先后顺序链. 有些时候，Touch后系统通过hit test 机制找到了触碰到的Initial View，但是Initial view并没有或者无法正常处理此次Touch。这个时候，系统便会通过响应者链寻找下一个响应者，以对此次Touc 进行响应。</p>
</blockquote>
<h2 id="Hit-Test机制"><a href="#Hit-Test机制" class="headerlink" title="Hit-Test机制"></a>Hit-Test机制</h2><blockquote>
<p>当你点击屏幕一个点时, 这个操作由硬件系统传递到操作系统, 然后封装为一个事件(Event) , 由UIApplication-&gt;UIWindows-&gt;RootView到最接近你手指的View来响应事件</p>
<p>子类超出父类边缘时候, 亦可以响应事件(正常是不可以的, 你需要重写<code>override func hitTest(_ point: CGPoint, with event: UIEvent?) -&gt; UIView?</code> 这个方法)</p>
</blockquote>
<h1 id="iOS关键字"><a href="#iOS关键字" class="headerlink" title="iOS关键字"></a>iOS关键字</h1><h2 id="static声明局部变量"><a href="#static声明局部变量" class="headerlink" title="static声明局部变量"></a>static声明局部变量</h2><blockquote>
<p>static 声明的局部变量, 会改变它的存储方式(生命周期), 使变量成为静态变量, 在编译时候就会分配内存, 知道程序退出才会释放内存. 这样就带有记忆的能力, 可以记录上一次的数据, 但是因为变量还是局部变量, 所有只能在其代码块内使用.</p>
</blockquote>
<h2 id="static-声明外部变量"><a href="#static-声明外部变量" class="headerlink" title="static 声明外部变量"></a>static 声明外部变量</h2><blockquote>
<p>用static声明外部变量，其本身就是静态变量，这只会改变其连接方式，使其只在本文件内部有效，而其他文件不可连接或引用该变量。</p>
</blockquote>
<h2 id="作用域函数"><a href="#作用域函数" class="headerlink" title="作用域函数"></a>作用域函数</h2><blockquote>
<p>使用static用于函数定义时，对函数的连接方式产生影响，使得函数只在本文件内部有效，对其他文件是不可见的。这样的函数又叫作静态函数。使用静态函数的好处是，不用担心与其他文件的同名函数产生干扰，另外也是对函数本身的一种保护机制。</p>
</blockquote>
<h1 id="网络部分"><a href="#网络部分" class="headerlink" title="网络部分"></a>网络部分</h1><h2 id="简单讲一下Socket"><a href="#简单讲一下Socket" class="headerlink" title="简单讲一下Socket"></a>简单讲一下Socket</h2><blockquote>
<p>Socket 套接字, 他本身不是协议, 是对TCP/IP协议的封装, 通过Socket才可以使用TCP/IP协议.<br>建立socket连接. 服务器监听-&gt;客户端进行请求-&gt;连接确认<br>在iOS中, 一般使用CocoaAsyncSocket 这个库来使用Socket</p>
</blockquote>
<h2 id="简单讲一下HTTP请求-以及GET和POST的区别"><a href="#简单讲一下HTTP请求-以及GET和POST的区别" class="headerlink" title="简单讲一下HTTP请求,以及GET和POST的区别"></a>简单讲一下HTTP请求,以及GET和POST的区别</h2><h3 id="HTTP-请求"><a href="#HTTP-请求" class="headerlink" title="HTTP 请求"></a>HTTP 请求</h3><p><a href="http://www.cnblogs.com/yin-jingyu/archive/2011/08/01/2123548.html" target="_blank" rel="external">这个Http请求讲的很明白</a></p>
<blockquote>
<p>HTTP是计算机通过网络进行通信的原则. HTTP是一中无状态协议(即Web服务器和Web服务器间不用建立持久的链接,当客户端发送请求,服务器端返回响应,链接就关闭了).</p>
<p>一次完整的通信过程 需要经历7个步骤</p>
</blockquote>
<ul>
<li>建立TCP链接</li>
<li>Web浏览器向Web服务器发送请求命令</li>
<li>Web浏览器发送请求头信息</li>
<li>Web服务器应答</li>
<li>Web服务器发送应答头信息</li>
<li>Web服务器向浏览器发送数据</li>
<li>Web服务器关闭TCP链接</li>
</ul>
<h3 id="GET和POST区别"><a href="#GET和POST区别" class="headerlink" title="GET和POST区别"></a>GET和POST区别</h3><h4 id="‘标准答案’"><a href="#‘标准答案’" class="headerlink" title="‘标准答案’"></a>‘标准答案’</h4><ul>
<li>GET在浏览器回退时是无害的，而POST会再次提交请求。</li>
<li>GET产生的URL地址可以被Bookmark，而POST不可以。</li>
<li>GET请求会被浏览器主动cache，而POST不会，除非手动设置。</li>
<li>GET请求只能进行url编码，而POST支持多种编码方式。</li>
<li>GET请求参数会被完整保留在浏览器历史记录里，而POST中的参数不会被保留。</li>
<li>GET请求在URL中传送的参数是有长度限制的，而POST么有。</li>
<li>对参数的数据类型，GET只接受ASCII字符，而POST没有限制。</li>
<li>GET比POST更不安全，因为参数直接暴露在URL上，所以不能用来传递敏感信息。</li>
<li>GET参数通过URL传递，POST放在Request body中。</li>
</ul>
<h4 id="装B答案"><a href="#装B答案" class="headerlink" title="装B答案"></a>装B答案</h4><blockquote>
<p>无论是GET还是POST 本质上都是TCP链接,并无差别. </p>
<p>但是他们有重要的区别在于GET方式请求产生一个TCP数据包;POST是两个.</p>
<p>对于GET方式的请求，浏览器会把http header和data一并发送出去，服务器响应200（返回数据</p>
<p>而对于POST，浏览器先发送header，服务器响应100 continue，浏览器再发送data，服务器响应200 ok（返回数据）。 并不是所有浏览器都会在POST中发送两次包，Firefox就只发送一次。在网络环境好的情况下，发一次包的时间和发两次包的时间差别基本可以无视。而在网络环境差的情况下，两次包的TCP在验证数据包完整性上，有非常大的优点。</p>
</blockquote>
<h2 id="HTTPS的加密方式"><a href="#HTTPS的加密方式" class="headerlink" title="HTTPS的加密方式"></a>HTTPS的加密方式</h2><p><a href="http://www.jianshu.com/p/56fc18937d09" target="_blank" rel="external">参考 1</a></p>
<h3 id="HTTPS的加密方式-1"><a href="#HTTPS的加密方式-1" class="headerlink" title="HTTPS的加密方式"></a>HTTPS的加密方式</h3><h4 id="对称加密"><a href="#对称加密" class="headerlink" title="对称加密"></a>对称加密</h4><blockquote>
<p>对称加密是指加密和解密使用相同密钥的加密算法。它要求发送方和接收方在安全通信之前，商定一个密钥。对称算法的安全性依赖于密钥，泄漏密钥就意味着任何人都可以对他们发送或接收的消息解密，所以密钥的保密性对通信至关重要。</p>
</blockquote>
<ul>
<li><p>优点：算法公开、计算量小、加密速度快、加密效率高。</p>
</li>
<li><p>缺点：         </p>
<ul>
<li><p>交易双方都使用同样钥匙，安全性得不到保证；</p>
<ul>
<li><p>每对用户每次使用对称加密算法时，都需要使用其他人不知道的惟一钥匙，这会使得发收信双方所拥有的钥匙数量呈几何级数增长，密钥管理成为用户的负担。</p>
</li>
<li><p>能提供机密性，但是不能提供验证和不可否认性。</p>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="非对称加密"><a href="#非对称加密" class="headerlink" title="非对称加密"></a>非对称加密</h4><blockquote>
<p>这种加密或许理解起来比较困难，这种加密指的是可以生成公钥和私钥。凡是公钥加密的数据，公钥自身不能解密，而需要私钥才能解密；凡是私钥加密的数据，私钥不能解密，需要公钥才能解密(比如说RSA)</p>
<p>非对称加密相比对称加密更加安全，但也存在两个明显缺点：</p>
</blockquote>
<ul>
<li><p>CPU计算资源消耗非常大。一次完全TLS握手，密钥交换时的非对称解密计算量占整个握手过程的90%以上。而对称加密的计算量只相当于非对称加密的0.1%，如果应用层数据也使用非对称加解密，性能开销太大，无法承受。</p>
</li>
<li><p>非对称加密算法对加密内容的长度有限制，不能超过公钥长度。比如现在常用的公钥长度是2048位，意味着待加密内容不能超过256个字节。</p>
</li>
</ul>
<blockquote>
<p>所以公钥加密目前只能用来作密钥交换或者内容签名，不适合用来做应用层传输内容的加解密</p>
</blockquote>
<h4 id="流程"><a href="#流程" class="headerlink" title="流程"></a>流程</h4><blockquote>
<p>首先服务器端用非对称加密（RSA）产生公钥和私钥。然后把公钥发给客 户端，路径或许有人会截取，但是没有用，因为用公钥加密的文件只有私钥可以解密，而私钥永远都不会离开服务器的。当公钥到达客户端之后，客户端会用对称加密产生一个秘钥并且用公钥来加密发送给服务器端，这个秘钥就是以后用来通信的钥匙。这样服务器端收到公钥加密的秘钥时就可以用私钥来解公钥从而获得秘钥。这样的话客户端和服务器端都获得了秘钥，信息交流相对是安全的</p>
</blockquote>
<div align="center"><br>    <img src="http://upload-images.jianshu.io/upload_images/535139-206dfc1d526b4b1f.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" align="center"> <br> </div> 

<blockquote>
<p>​ 听起来确实是挺安全的，但实际上，还有一种更恶劣的攻击是这种方法无 法防范的，这就是传说中的“中间人攻击”。在身份认证的过程中，出现了一个“中间人”拦截我们的信息，他有意想要知道你们的消息。我们将这个中间人称为M。当服务器第一次给客户端发送公钥的时候，途径M。M知道你要进行密钥交换了，它把公钥扣了下来，假装自己是客户端，伪造了一个伪秘钥（对称加密产生的），然后用服务器发来的公钥加密了伪秘钥发还给服务器，这样服务器以为和客户端完成了密钥交换，实际上服务器是和M完成了密钥交换（获得了伪秘钥）。同时M假扮成服务器自行用非对称加密产生伪公钥和伪私钥，与客户端进行秘钥交换，拿到客户端发送过来的秘钥。现在客户端拿着秘钥，M拿着秘钥和为伪秘钥，服务器拿着伪秘钥，整个交流的过程就是：</p>
</blockquote>
<div align="center"><br>    <img src="https://user-gold-cdn.xitu.io/2017/6/21/5d38312283e1e59840315ba2099aff38 " align="center"> <br> </div> 

<blockquote>
<p>这种问题就需要CA证书去解决. (应该可以满足大部分面试问题不再过多深入)</p>
</blockquote>
<h1 id="持续化存储方式"><a href="#持续化存储方式" class="headerlink" title="持续化存储方式"></a>持续化存储方式</h1><ul>
<li>NSUserDefaults</li>
<li>plist</li>
<li>NSKeyedArchiver</li>
<li>SQL</li>
<li>CoreData</li>
<li>Realm(新兴数据库, 暂时没接触)</li>
</ul>
<h1 id="一些三方实现过程"><a href="#一些三方实现过程" class="headerlink" title="一些三方实现过程"></a>一些三方实现过程</h1><h2 id="SDWebImage"><a href="#SDWebImage" class="headerlink" title="SDWebImage"></a>SDWebImage</h2><p><a href="http://www.jianshu.com/p/733954fce261" target="_blank" rel="external">引用</a></p>
<ol>
<li>入口 setImageWithURL:placeholderImage:options: 会先把 placeholderImage 显示，然后 SDWebImageManager 根据 URL 开始处理图片。</li>
<li>进入 SDWebImageManager-downloadWithURL:delegate:options:userInfo:，交给 SDImageCache 从缓存查找图片是否已经下载 queryDiskCacheForKey:delegate:userInfo:.</li>
<li>先从内存图片缓存查找是否有图片，如果内存中已经有图片缓存，SDImageCacheDelegate 回调 imageCache:didFindImage:forKey:userInfo: 到 SDWebImageManager。</li>
<li>SDWebImageManagerDelegate 回调 webImageManager:didFinishWithImage: 到 UIImageView+WebCache 等前端展示图片。</li>
<li>如果内存缓存中没有，生成 NSInvocationOperation 添加到队列开始从硬盘查找图片是否已经缓存。</li>
<li>根据 URLKey 在硬盘缓存目录下尝试读取图片文件。这一步是在 NSOperation 进行的操作，所以回主线程进行结果回调 notifyDelegate:。</li>
<li>如果上一操作从硬盘读取到了图片，将图片添加到内存缓存中（如果空闲内存过小，会先清空内存缓存）。SDImageCacheDelegate 回调 imageCache:didFindImage:forKey:userInfo:。进而回调展示图片。</li>
<li>如果从硬盘缓存目录读取不到图片，说明所有缓存都不存在该图片，需要下载图片，回调 imageCache:didNotFindImageForKey:userInfo:。</li>
<li>共享或重新生成一个下载器 SDWebImageDownloader 开始下载图片。</li>
<li>图片下载由 NSURLConnection 来做，实现相关 delegate 来判断图片下载中、下载完成和下载失败。</li>
<li>connection:didReceiveData: 中利用 ImageIO 做了按图片下载进度加载效果。</li>
<li>connectionDidFinishLoading: 数据下载完成后交给 SDWebImageDecoder 做图片解码处理。</li>
<li>图片解码处理在一个 NSOperationQueue 完成，不会拖慢主线程 UI。如果有需要对下载的图片进行二次处理，最好也在这里完成，效率会好很多。</li>
<li>在主线程 notifyDelegateOnMainThreadWithInfo: 宣告解码完成, imageDecoder:didFinishDecodingImage:userInfo: 回调给 SDWebImageDownloader。</li>
<li>imageDownloader:didFinishWithImage: 回调给 SDWebImageManager 告知图片下载完成。</li>
<li>通知所有的 downloadDelegates 下载完成，回调给需要的地方展示图片。</li>
<li>将图片保存到 SDImageCache 中，内存缓存和硬盘缓存同时保存。写文件到硬盘也在以单独 NSInvocationOperation 完成，避免拖慢主线程。</li>
<li>SDImageCache 在初始化的时候会注册一些消息通知，在内存警告或退到后台的时候清理内存图片缓存，应用结束的时候清理过期图片。</li>
<li>SDWI 也提供了 UIButton+WebCache 和 MKAnnotationView+WebCache，方便使用。</li>
<li>SDWebImagePrefetcher 可以预先下载图片，方便后续使用。</li>
</ol>
<h1 id="优化问题"><a href="#优化问题" class="headerlink" title="优化问题"></a>优化问题</h1><h2 id="iOS离屏渲染"><a href="#iOS离屏渲染" class="headerlink" title="iOS离屏渲染"></a>iOS离屏渲染</h2><blockquote>
<p>直接将图层合成到帧的缓冲区中(在屏幕上)比先创建屏幕外缓冲区，然后渲染到纹理中，最后将结果渲染到帧的缓冲区中要廉价很多。因为这其中涉及两次昂贵的环境转换(转换环境到屏幕外缓冲区，然后转换环境到帧缓冲区)。触发离屏渲染后这种转换发生在每一帧，在界面的滚动过程中如果有大量的离屏渲染发生时会严重影响帧率</p>
</blockquote>
<h3 id="iOS中可能会触发离屏渲染的操作"><a href="#iOS中可能会触发离屏渲染的操作" class="headerlink" title="iOS中可能会触发离屏渲染的操作"></a>iOS中可能会触发离屏渲染的操作</h3><ul>
<li>mask</li>
<li>shadow </li>
<li>group opacity</li>
<li>edge antialiasing</li>
</ul>
<blockquote>
<p>在iOS 8之前 Core Graphics 也会触发离屏渲染, 但是不是GPU离屏渲染是, Core Graphics 绘制 API 是在 CPU 上执行，触发的是 CPU 版本的离屏渲染。</p>
</blockquote>
<h3 id="layer-cornerRaius-gt-0-是否会触发离屏渲染"><a href="#layer-cornerRaius-gt-0-是否会触发离屏渲染" class="headerlink" title="layer.cornerRaius &gt; 0 是否会触发离屏渲染"></a>layer.cornerRaius &gt; 0 是否会触发离屏渲染</h3><blockquote>
<p>答案是不会. 只有它和<code>layer.masksToBounds = true</code> 一起才会触发</p>
</blockquote>
<h3 id="shouldRasterize-YES-是否会触发呢"><a href="#shouldRasterize-YES-是否会触发呢" class="headerlink" title="shouldRasterize = YES 是否会触发呢"></a>shouldRasterize = YES 是否会触发呢</h3><blockquote>
<p>是</p>
<p>但是他较为特殊, 他会将离屏渲染的的同时, 会将光栅化的内容缓存起来, 如果layer 或者 SubLayer 未发生变化, 下一帧会拿来直接使用. </p>
<p>当你使用光栅化时，你可以开启“Color Hits Green and Misses Red”来检查该场景下光栅化操作是否是一个好的选择。绿色表示缓存被复用，红色表示缓存在被重复创建。</p>
<p>对于经常变动的内容,这个时候不要开启,否则会造成性能的浪费(比如TableView)</p>
</blockquote>
<h1 id="Crash问题"><a href="#Crash问题" class="headerlink" title="Crash问题"></a>Crash问题</h1><p><a href="http://www.open-open.com/lib/view/open1481531504413.html" target="_blank" rel="external">参考</a></p>
<h2 id="什么情况下会产生崩溃日志"><a href="#什么情况下会产生崩溃日志" class="headerlink" title="什么情况下会产生崩溃日志"></a>什么情况下会产生崩溃日志</h2><ul>
<li>应用中有Bug 崩溃</li>
<li>Watchdog 超时机制. 如果用户强制退出</li>
<li>低内存日志</li>
</ul>
<h2 id="Crash收集的几种方式"><a href="#Crash收集的几种方式" class="headerlink" title="Crash收集的几种方式"></a>Crash收集的几种方式</h2><ul>
<li>简单可以自己写一个方法 之后在写入词磁盘中, 下次进入时候上传服务器, 之后删除</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">void handleException(NSException *exception) &#123;</div><div class="line">	 NSArray *callStack = [exception callStackSymbols];</div><div class="line">    NSString *reason = [exception reason];</div><div class="line">    NSString *name = [exception name];</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<ul>
<li>使用Xcode自带的工具.Windows-&gt;Organizer-&gt;选择你需要的App-&gt;Crashes会自己动下载崩溃 直接打开就可以了</li>
<li>使用第三方框架例如Bugly等</li>
</ul>
<h2 id="Crash分析"><a href="#Crash分析" class="headerlink" title="Crash分析"></a>Crash分析</h2><blockquote>
<p>一般采用 .Crash 文件 + .dSYM文件 + symbolicatecrash(Xcode自带工具) 输出.log<br>详细请看参考 )</p>
</blockquote>
<h1 id="其他知识"><a href="#其他知识" class="headerlink" title="其他知识"></a>其他知识</h1><h2 id="在不知道二进制文件格式情况下如何区分文件"><a href="#在不知道二进制文件格式情况下如何区分文件" class="headerlink" title="在不知道二进制文件格式情况下如何区分文件"></a>在不知道二进制文件格式情况下如何区分文件</h2><blockquote>
<p>可以通过二进制头识别文件类型(说几个简单的 不好意思 一个没记住)或者UE等工具打开</p>
</blockquote>
<p><a href="http://blog.csdn.net/yagerfgcs/article/details/51427085" target="_blank" rel="external">参考网址</a></p>
<h1 id="lldb常用操作"><a href="#lldb常用操作" class="headerlink" title="lldb常用操作"></a>lldb常用操作</h1><p><a href="http://www.dsperson.com/2017/06/14/lldb%E5%AD%A6%E4%B9%A0/" target="_blank" rel="external">查看我以前的文章</a></p>
]]></content>
      <categories>
        <category>面试</category>
      </categories>
  </entry>
</search>
